<form method="post" action="{{ path('app_control_delete', {'id': control.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ control.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>

