<!-- Page Wrapper -->
    <div id="wrapper">

        <!-- Sidebar -->
        <ul class="navbar-nav bg-gradient-secondary sidebar sidebar-dark accordion" id="accordionSidebar">

            <!-- Sidebar - Brand -->
            <a class="sidebar-brand d-flex align-items-center justify-content-center" href="{{ url('app_reponse_index') }}">
                <div class="sidebar-brand-icon rotate-n-15">
                    <img class="img-profile rounded-circle"
                                    src={{ asset('icons/icons8-g-key-48.png') }}>
                </div>
                <div class="sidebar-brand-text mx-3">Gestion Incident</div>
            </a>

            <!-- Divider -->
            <hr class="sidebar-divider my-0">

            <!-- Nav Item - Dashboard -->
            <li class="nav-item active">
                <a class="nav-link" href="{{ url('app_reponse_index') }}">
                    <i class="fas fa-fw fa-tachometer-alt"></i>
                    <span>FAQ</span></a>
            </li>
             
            <!-- Divider -->
            <hr class="sidebar-divider">
            {% if is_granted('ROLE_USER') %}
            <!-- Heading -->
            <div class="sidebar-heading">
                Utilisateur {{app.user.Nom}}
            </div>

            <!-- Nav Item - Tables -->
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_user_show') }}">
                    <i class="fas fa-fw fa-user-circle"></i>
                    <span>Profile</span></a>
            </li>

            <!-- Nav Item - Utilities Collapse Menu -->
            <li class="nav-item">
                <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseUtilities"
                    aria-expanded="true" aria-controls="collapseUtilities">
                    <i class="fas fa-fw fa-question-circle"></i>
                    <span>Vos Questions </span>
                </a>
                <div id="collapseUtilities" class="collapse" aria-labelledby="headingUtilities"
                    data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <h6 class="collapse-header">Vos Questions:</h6>
                        <a class="collapse-item" href="{{ url('app_question_en_cours') }}">En cours</a>
                        <a class="collapse-item" href="{{ url('app_question_repondue') }}">Terminé </a>
                </div>
            </li>
            {% endif %} 
            {% if is_granted('ROLE_UAPP') %}
            <!-- Nav Item - Tables -->
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_fonc_index') }}">
                    <i class="fas fa-fw fa-laptop"></i>
                    <span>Fonctionnalite</span></a>
            </li>
            {% endif %} 
            {% if is_granted('ROLE_REP') %}
             <!-- Divider -->
            <hr class="sidebar-divider">

            <!-- Heading -->
            <div class="sidebar-heading">
                Responsable
            </div>

            <!-- Nav Item - Pages Collapse Menu -->
            <li class="nav-item">
                <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseTwo"
                    aria-expanded="true" aria-controls="collapseTwo">
                    <i class="fas fa-fw fa-question"></i>
                    <span>Les Questions</span>
                </a>
                <div id="collapseTwo" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <h6 class="collapse-header">Les Questions:</h6>
                        <a class="collapse-item" href="{{ path('app_responsable_index') }}">En cours</a>
                        <a class="collapse-item" href="{{ url('app_responsable_terminé') }}">Terminé</a>
                    </div>
                </div>
            </li>
            {% endif %}

            <!-- Divider -->
            <hr class="sidebar-divider">
            {% if is_granted('ROLE_ADMIN') %}

            <!-- Heading -->
            <div class="sidebar-heading">
                Administrateur
            </div>

            <!-- Nav Item - Pages Collapse Menu -->
            <li class="nav-item">
                <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapsePages"
                    aria-expanded="true" aria-controls="collapsePages">
                    <i class="fas fa-fw fa-folder"></i>
                    <span>Gestion</span>
                </a>
                <div id="collapsePages" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <h6 class="collapse-header">Gestion:</h6>
                        <a class="collapse-item" href="{{ url('app_admin_utilisateur') }}">Utilisateur</a>
                        <div class="collapse-divider"></div>
                        <h6 class="collapse-header">Pages:</h6>
                        <a class="collapse-item" href="{{ url('app_application_index') }}">Application</a>
                        <a class="collapse-item" href="{{ url('app_control_index') }}">Controller</a>
                        <a class="collapse-item" href="{{ url('app_rout_index') }}">Route</a>
                        <a class="collapse-item" href="{{ url('app_fonc_index') }}">Fonctionnalite</a>
                    </div>
                </div>
            </li>

            <!-- Nav Item - Charts -->
            {# <li class="nav-item">
                <a class="nav-link" href="{{ url('app_question_indexG') }}">
                    <i class="fas fa-fw fa-chart-area"></i>
                    <span>Historique</span></a>
            </li> #}
            {% endif %}

            <!-- Divider -->
            <hr class="sidebar-divider">

            <!-- Nav Item - Charts -->
            <li class="nav-item">
                <a class="nav-link" href="{{ url('app_login') }}">
                    <i class="fas fa-sign-in-alt fa-fw"></i>
                    <span>Connexion</span></a>
            </li>

            <!-- Divider -->
            <hr class="sidebar-divider d-none d-md-block">

            <!-- Sidebar Toggler (Sidebar) -->
            <div class="text-center d-none d-md-inline">
                <button class="rounded-circle border-0" id="sidebarToggle"></button>
            </div>

           

        </ul>
        <!-- End of Sidebar -->

        <!-- Content Wrapper -->
        <div id="content-wrapper" class="d-flex flex-column">

            <!-- Main Content -->
            <div id="content">

                <!-- Topbar -->
                <nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">

                    {% if is_granted('ROLE_USER') %}
                    <a href="{{ path('app_question_new') }}" class="d-none d-sm-inline-block btn btn-sm btn-theme shadow-sm">
					Poser une question <i class="fas fa-fw fa-question"></i></a>
				{% endif %}

                    <!-- Topbar Navbar -->
                    <ul class="navbar-nav ml-auto">

                        <!-- Nav Item - Search Dropdown (Visible Only XS) -->
                        <li class="nav-item dropdown no-arrow d-sm-none">
                            <a class="nav-link dropdown-toggle" href="#" id="searchDropdown" role="button"
                                data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <i class="fas fa-search fa-fw"></i>
                            </a>
                            <!-- Dropdown - Messages -->
                            <div class="dropdown-menu dropdown-menu-right p-3 shadow animated--grow-in"
                                aria-labelledby="searchDropdown">
                                <form class="form-inline mr-auto w-100 navbar-search">
                                    <div class="input-group">
                                        <input type="text" class="form-control bg-light border-0 small"
                                            placeholder="Search for..." aria-label="Search"
                                            aria-describedby="basic-addon2">
                                        <div class="input-group-append">
                                            <button class="btn btn-primary" type="button">
                                                <i class="fas fa-search fa-sm"></i>
                                            </button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </li>

                        <!-- Nav Item - Inscription -->
                        <li class="nav-item dropdown no-arrow">
                            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button"
                                data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <span class="mr-2 d-none d-lg-inline text-gray-600 small">Inscription Ici</span>
                                
                            </a>
                            <!-- Dropdown - User Information -->
                            <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in"
                                aria-labelledby="userDropdown">
                                <a class="dropdown-item" href="{{ path('app_register') }}">
                                    <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
                                    S'inscrire
                                </a>
                                <a class="dropdown-item" href="{{ path('app_login') }}">
                                    <i class="fas fa-cogs fa-sm fa-fw mr-2 text-gray-400"></i>
                                    connexion
                                </a>
                            </div>
                        </li>

                        <div class="topbar-divider d-none d-sm-block"></div>
                        {% if is_granted('ROLE_USER') %}
                        <!-- Nav Item - User Information -->
                        <li class="nav-item dropdown no-arrow">
                            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button"
                                data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <span class="mr-2 d-none d-lg-inline text-gray-600 small">{{app.user.Nom}}</span>
                                <img class="img-profile rounded-circle"
                                    src={{ asset('assets/img/undraw_profile.svg') }}>
                            </a>
                            <!-- Dropdown - User Information -->
                            <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in"
                                aria-labelledby="userDropdown">
                                <a class="dropdown-item" href="{{ path('app_user_show') }}">
                                    <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
                                    Profile
                                </a>
                                <a class="dropdown-item" href="{{ path('app_user_edit') }}">
                                    <i class="fas fa-cogs fa-sm fa-fw mr-2 text-gray-400"></i>
                                    Settings
                                </a>
                                <a class="dropdown-item" href="{{ url('app_question_index') }}">
                                    <i class="fas fa-list fa-sm fa-fw mr-2 text-gray-400"></i>
                                    Question en cours
                                </a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="#" data-toggle="modal" data-target="#logoutModal">
                                    <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                                    Logout
                                </a>
                            </div>
                        </li>
                        {% endif %}

                    </ul>

                </nav>
    
                <!-- End of Topbar -->